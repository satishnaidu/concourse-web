---
platform: linux

image_resource:
  type: docker-image
  source: 
    repository: maven
    tag: 3.5.4-jdk-8

inputs:
  - name: version
  - name: concourse-web

outputs:
  - name: artifactory-repository

params:
  ARTIFACTORY_USER:
  ARTIFACTORY_PW:
  ARTIFACTORY_URL:

run:
  path: ./concourse-web/ci/scripts/run_build.sh
  #path: ls
  #args: [-R]


